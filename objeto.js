//Array funciona melhor quando queremos guardar apenas um tipo de dado

//Objeto imita a realidade, são conjuntos de dados diferentes, é uma forma de tentar refletir de forma abstrada as coisas da vida real.

const objPessoa = {
    nome: 'Jose Silva',
    idade: 32,
    cpf: 12345678912,
    turma: 'JavaScript'
}
console.log(objPessoa.nome);